# Backendul pe care il foloseste build-ul. Fara el, `dev` cade pe zona de test
# (localhost:3999, vezi docker-compose.test.yml) si `build` pe productie.
#
#   zona de test   http://localhost:3999
#   productie      https://api.shelfshare.ro
VITE_API_BASE_URL=http://localhost:3999
